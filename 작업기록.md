# 나무 링크런 작업 기록

친구들끼리 원격으로 즐기는 나무위키 링크 스피드런 도우미.

- **사이트 (로그인 없이 누구나)**: https://nrinbal.github.io/namu-linkrun/ — 방은 주소 끝 `#방이름`
  - 저장소: https://github.com/NRinBal/namu-linkrun (Pages: main 브랜치 `/docs`)
  - 저장소 데이터: Firebase 프로젝트 `namu-linkrun`, Firestore (서울)
  - **수정 후 반영 순서**: 파일 수정 → `python build.py` → `git add -A` → `git commit` → `git push` → 1~2분 뒤 사이트 반영
- claude.ai 버전: https://claude.ai/artifact/U2pV3dS8xD8dNNUPfVJxwG
  - 친구 초대: 페이지 공유 메뉴 → 이메일로 초대, **편집 가능** 권한 (보기 권한은 기록 저장 불가)

## 파일

| 파일 | 설명 |
|---|---|
| `linkrun.html` | 게임 페이지 (claude.ai 아티팩트로 게시). 공유 저장소(db)로 라운드·참가자·기록 동기화 |
| `linkrun.user.js` | 나무위키용 자동 기록 스크립트. Tampermonkey용이면서 북마크 버튼의 원본. 아티팩트에 같이 게시되어 페이지에서 복사 가능 |

**북마크 버튼 방식:** 게임 페이지가 페이지에 들어 있는 스크립트로 `toBookmarklet()`로 북마크 코드를 만듦. 코드 구성은 이래요.
- `__LINKRUN_BM` 정의
- `GM_getValue`·`GM_setValue`를 나무위키 localStorage로, `GM_setClipboard`를 `navigator.clipboard`로 흉내 내는 감싸개
- 스크립트 본문 (헤더와 `//` 주석 줄은 뺌)
- 전체를 `encodeURIComponent`로 인코딩

스크립트를 고치면 북마크 코드도 자동으로 바뀜. 단, 친구들은 북마크를 새로 만들어야 함.
나무위키에는 CSP가 없어서 북마크 코드 실행이 막히지 않음 (2026-09-23 확인).

| `docs/index.html` | 사이트 버전 (GitHub Pages로 올림). `build.py`가 `linkrun.html`로 만듦 — 직접 고치지 말 것 |
| `docs/firebase-config.js` | Firebase 웹 앱 설정값. 비어 있으면(null) 혼자 연습 모드 |
| `firestore.rules` | Firestore 보안 규칙. Firebase 콘솔 규칙 탭에 붙여넣음 |
| `pool.json` | 랜덤 출제 문서 목록 (분류별). 고친 뒤 `build.py` 실행 |
| `bans.json` | 금지 문서 묶음 (국가, 연도·날짜, 언어, 주요 도시, 대형 허브). 고친 뒤 `build.py` 실행 |
| `build.py` | `linkrun.user.js` 내용을 `linkrun.html` 안의 `<script type="text/plain" id="userscript-src">`에 넣음 |

**스크립트를 고친 뒤에는 `python build.py`를 실행하고 나서 `linkrun.html`을 게시해야 함.** 게임 페이지는 스크립트를 따로 불러오지 않고 페이지 안에 든 내용을 씀.

## 구조

**공유 저장소 (db)**
- `game/state` — 현재 라운드: `round, status(lobby|running|ended), start, target, startAt, rules{back,foot}, host`
- `players/<pid>` — 참가자별 기록: `name, round, path[{t, ms, how}], finishedMs, gaveUp, auto`
- `history/r<번호>` — 끝난 라운드 결과

참가자 식별은 브라우저 localStorage의 무작위 id (`linkrun.pid`). 다른 브라우저로 열면 다른 사람으로 인식됨.

**자동 기록 흐름**
1. 게임 페이지의 "참가하고 시작 문서 열기" 링크에 `#linkrun=<base64 JSON {r,s,t,a,p}>` 를 붙여 나무위키를 엶
2. 스크립트가 해시를 읽어 라운드 정보를 저장(GM storage)하고 해시를 지움
3. 0.25초마다 URL 변화를 확인 (나무위키는 SPA라 페이지가 새로 로드되지 않음)
4. 결과 코드 `LR1.<base64 JSON>.<FNV-1a 체크섬>` 을 게임 페이지 입력칸에 붙여넣으면 기록 반영

**이동 분류 (`how`)**
- `l` 본문 링크 — 클릭한 링크가 "h1 제목과 분류 링크를 함께 감싸는 가장 작은 영역" 안에 있음
- `o` 본문 밖 링크 — 오른쪽 최근 변경, 각주 팝업 등
- `b` 뒤로/앞으로 — popstate 직후 도착
- `j` 링크 없이 이동 — 검색창, 주소창, 랜덤 문서 등
- `m` 수동 입력 (스크립트 없이 제목을 직접 기록)

나무위키 클래스 이름은 무작위 해시라 쓰지 않음. 사이트 개편으로 분류가 틀어지면 `contentRoot()` 를 먼저 확인.

## 변경 이력

### 2026-09-23
- v1: 게임 페이지 최초 제작 (출제, 5초 카운트다운, 수동 경로 기록, 실시간 순위, 지난 라운드)
- v2: "참가하고 시작 문서 열기" 버튼이 안 눌리던 버그 수정
  - 원인: 0.1초 타이머 갱신 때 버튼까지 매번 새로 만들어서 클릭이 끊김 → 타이머 글자만 갱신하도록 분리
  - 위쪽 "시작 문서" 칸도 클릭해서 참가 가능
- v3: 자동 기록 스크립트 추가
  - 나무위키 오른쪽 아래 기록판 (타이머, 목표, 경로, 경고 수, 포기, 결과 코드 복사)
  - 게임 페이지: 결과 코드 붙여넣기, "자동"/"경고 N" 배지, 경로에 이동 방식 표시, 스크립트 설치 안내
  - 실제 나무위키에서 확인: 본문 링크(l), 최근 변경 링크(o), 뒤로 가기(b), 검색(j), 목표 도착 시 완주 처리 → 모두 정상
  - 확인 못 한 것: 실제 Tampermonkey 환경에서의 기록판 화면(GM API를 흉내 내서 추적 로직만 시험함), 여러 명 동시 플레이
- v4: Tampermonkey 없이 쓰는 **북마크 버튼** 방식 추가 (스크립트 1.1.0)
  - 게임 페이지: "북마크 코드 복사" 버튼 + 만드는 방법. Tampermonkey는 "방법 2"로 접어 둠
  - 스크립트: 두 번 실행 방지(`data-linkrun` 표시). 북마크를 다시 누르면 기록판만 다시 펼침
  - 라운드 없이 북마크를 누르면 안내창 띄움
  - 새로고침 뒤 북마크를 늦게 눌러도, 도착 시각을 페이지를 불러온 시각으로 기록
  - 자동 복사가 막히면 기록판에 코드 칸이 떠서 직접 복사 가능 (시험 환경에서 실제로 막혔고, 대체 칸이 뜨는 것 확인)
  - 확인한 것: 생성된 북마크 코드 문법, 나무위키에서 시작 정보 읽기, 본문 링크로 목표 도착 → 완주, 결과 코드 생성
  - 게임 페이지(로컬 모드): 결과 코드를 받아 순위 1위에 "자동" 배지로 표시됨. 체크섬이 틀린 코드는 거부됨
  - 확인 못 한 것: 실제 브라우저 북마크바에 저장해서 누르기, 기록판의 실제 화면 위치(시험 창이 숨겨져 있어 화면 캡처 불가)
- v5: "북마크 코드를 아직 불러오는 중" 문제 수정
  - 원인: 게시된 페이지에서 따로 올린 `linkrun.user.js`를 `fetch`로 읽지 못함. 로컬에서는 됐지만 claude.ai 뷰어에서는 안 됨
  - 해결: 스크립트를 페이지 안(`<script type="text/plain">`)에 직접 넣음. `build.py`로 동기화
  - 로컬에서 북마크 코드 생성과 문법 확인. 실제 게시 페이지에서 버튼 동작은 사용자 확인 필요 → 사용자가 잘 된다고 확인함
- v6: **claude.ai 계정 없이 하는 사이트 버전** 준비 (GitHub Pages + Firebase Firestore)
  - `linkrun.html` 하나로 두 버전을 같이 관리
    - `LINKRUN_FIREBASE`가 정의돼 있으면 사이트 모드 → Firestore 사용
    - 아니면 claude.ai 모드 → `claude.use("db")` 사용
  - Firestore compat SDK의 `doc/collection/onSnapshot/set/update`가 claude.ai db와 거의 같아서 저장 코드는 그대로 씀
  - 방 나누기: 주소 끝 `#방이름` → Firestore 경로 `rooms/<방이름>/game|players|history`. 없으면 `main`
  - 사이트 모드에만 방 표시줄(방 링크 복사, 다른 방으로 이동)이 보임
  - 로그인이 없어서 링크를 아는 사람은 누구나 기록을 쓸 수 있음. `firestore.rules`로 정해진 경로 밖 쓰기, 삭제, 너무 큰 문서만 막음
  - Firebase 설정값(apiKey 등)은 원래 공개되는 값이라 저장소에 올려도 됨. 보안은 규칙이 담당
  - 확인한 것(로컬, 설정 전): 페이지 로드, Firebase SDK 로드, 방 이름 인식, 혼자 연습 모드 안내, 북마크 코드 문법
  - 확인 못 한 것: 실제 Firestore 연결과 동기화 (프로젝트 생성 후 확인 필요)
- Firebase 프로젝트 `namu-linkrun` 연결 (설정값은 `docs/firebase-config.js`, measurementId는 안 써서 뺌)
  - 로컬 사이트에서 확인한 것
    - 방 `claude-test`에 읽기·쓰기 성공
    - 규칙 밖 경로(`secret/x`) 쓰기는 permission-denied로 막힘 → 규칙 적용됨
    - 두 창 실시간 동기화: 한 창에서 라운드 시작 → 다른 창이 보고 참가·완주 → 첫 창 순위판에 1위로 뜸
    - 라운드 종료 → 지난 라운드 기록 저장
  - GitHub Pages 배포 후 실제 주소에서 다시 확인
    - Firestore 연결됨
    - 두 창 실시간 동기화: 라운드 공개와 참가자 목록이 양쪽에 보임
    - 라운드 종료 → 기록 저장
    - 북마크 코드 생성
  - 방 `claude-test`에 테스트 기록이 남아 있음 (1·2라운드).
- v7 (스크립트 1.2.0): **넘겨주기(리다이렉트) 문서 완주 인식** 수정
  - 증상: 목표 `세종대왕`, 실제 문서는 `세종(조선)`. 본문 링크가 `/w/세종(조선)`을 바로 가리키면 주소에 `?from=`이 안 붙어서 완주가 인식되지 않음
  - 해결
    - 라운드 시작 때 같은 사이트의 숨은 iframe으로 목표 문서를 한 번 열어 실제 제목을 알아둠 (`run.tr`)
    - 기록판에 "넘겨주기 확인 중" → "세종대왕 (= 세종(조선))"으로 표시
    - 확인 전에 이미 도착했으면 확인이 끝날 때 완주 처리
  - 시작 문서가 넘겨주기인 경우(`한국` → `대한민국?from=한국`)도, 첫 도착을 이동으로 세지 않도록 고침 (`run.sr`)
  - 확인: `훈민정음`에서 시작, 목표 `세종대왕`. 1~2초 안에 `세종(조선)` 확인 → 본문의 `/w/세종(조선)` 링크 클릭 → 1클릭 완주
  - 남은 한계: 게임 페이지에서 제목을 직접 입력하는 방식은 넘겨주기를 모름 (`세종(조선)`을 입력해도 완주 안 됨). 자동 기록에서만 해결됨
  - **북마크·Tampermonkey 스크립트는 각자 가진 사본이라, 새 코드로 북마크를 다시 만들어야 적용됨** → 사용자가 다시 만들고 정상 동작 확인
- v8: **랜덤 출제 문서 75개 → 360개**, 분류 선택 추가
  - 분류 9개 × 40개: 음식, 동물, 과학·기술, 인물, 장소, 역사, 게임·애니·영화, 스포츠, 문화·생활
  - 출제 칸에 "랜덤 시작 분류", "랜덤 목표 분류" 선택 추가 (기본 전체). 고른 분류는 브라우저에 기억됨
  - 목록은 `pool.json`에 두고 `build.py`가 페이지에 넣음. 중복이나 빈 분류가 있으면 빌드가 멈춤
  - 검증: 후보 360개를 나무위키에서 숨은 iframe으로 하나씩 열어 확인
    - 없는 문서 3개와 동음이의어 문서 2개(`말`, `파리`)는 다른 제목으로 바꾸고 다시 확인
    - 넘겨주기 30개는 실제 제목으로 저장 (예: `세종대왕` → `세종(조선)`, `페이커` → `이상혁`). 제목을 직접 입력하는 방식에서도 완주가 맞게 인식됨
  - 확인: 분류 선택지 10개(전체 포함) 표시. 음식→인물로 200번 뽑아 모두 해당 분류에서 나옴. 사이트 모드 Firestore 연결 정상
  - 나무위키 문서 제목은 바뀔 수 있음. 나중에 없는 문서가 뽑히면 `pool.json`에서 고치면 됨
- v9 (스크립트 1.3.0): **금지 문서 모드**
  - 출제 칸에서 금지 묶음 선택
    - 국가 41, 연도·날짜(규칙), 언어 12, 주요 도시 19, 대형 허브 20
    - 직접 입력 가능 (최대 100개)
    - "경고만 붙이기" 또는 "바로 실격" 선택
    - 출제자 브라우저에 설정 기억
  - 저장 위치: `rules.ban = {keys, custom, titles, patterns, skip, mode}`
    - 시작 링크, 순위판, 지난 라운드에 자동으로 같이 전달됨
    - `skip` = 시작·목표 문서라서 금지에서 빠짐 (예: 목표 `1945년` + 연도 금지여도 완주 가능)
  - 연도·날짜 규칙 `BAN_PATTERNS.date`: `N년`, `N년대`, `N세기`, `기원전 …`, `M월 D일`, `M월`
    - 스크립트와 페이지 두 곳에 같은 규칙이 있어서 **고칠 때 둘 다 고쳐야 함**
  - 스크립트
    - 금지 문서 도착 → `how='x'`. 실격 모드면 `run.x = true`로 기록 멈춤
    - 금지 링크에 빨간 취소선 (`a[data-lr-ban="1"]`, 0.7초마다 갱신)
    - 넘겨주기로 들어간 경우도 `?from=`으로 잡음
  - 페이지
    - 금지 문서 표시줄과 목록 보기
    - 실격 상태 (순위 맨 아래, 입력 잠금)
    - 경로에 "금지 문서" 태그. 옛 스크립트나 직접 입력 기록도 제목으로 다시 판정
  - 확인
    - 페이지(로컬 + Firestore): 판정 9가지 정상 (시작·목표 제외, `3.1 운동`은 날짜 아님)
    - 페이지: 시작 링크에 금지 목록 42개와 규칙이 담김. `대한민국` 입력 → 실격, 입력 잠금, 순위 반영
  - 확인 (나무위키 + 실제 스크립트 파일, 임시 브랜치에서 불러옴)
    - `미국` 문서에서 금지 링크 519개에 취소선
    - 목표 `1945년`과 금지 아닌 `워싱턴 D.C.`는 취소선 없음
    - `캐나다` 클릭 → 금지 문서로 기록, 실격, 취소선 해제
  - **스크립트가 바뀌어서 북마크를 다시 만들어야 함** (옛 북마크는 취소선·실격 표시는 없지만, 페이지가 결과 코드의 경로로 다시 판정함)
- v10 (스크립트 1.3.1): **디자인 개편 "하이퍼링크 레이스"**
  - 글꼴 통일: Google Fonts **Gothic A1** 하나. 굵기(400~900)로만 위계를 줌
    - 옛 Black Han Sans, IBM Plex Sans KR, JetBrains Mono는 뺌
    - 코드 칸(북마크 코드, 스크립트 내용)만 시스템 고정폭 글꼴
  - 모티프: 하이퍼링크
    - 시작 문서 = 파란 밑줄 링크, 목표 문서 = 형광펜
    - 지나온 경로 = 방문한 링크 보라색, 도착 문서 = 형광펜 칩
  - 무대(시작→목표, 타이머)만 두꺼운 테두리와 딱딱한 그림자로 띄움. 아래 판넬은 옅은 테두리만
  - 버튼: 두꺼운 테두리, 누르면 그림자만큼 들어가는 효과. 타이머는 최대 112px, 숫자 폭 고정
  - 색 토큰: 밝은/어두운 테마 각각 정의
    - 목표 형광펜은 `--target-ink`/`--target-mark`로 따로 둠. 어두운 테마에서는 노란 글씨 + 옅은 형광펜
  - 버그 수정: 머리 소개글이 작은 태그(`.tag`)와 같은 클래스라 태그 모양으로 보이던 문제 → `.tagline`
  - 나무위키 기록판도 같은 색·글꼴로 바꿈 (Gothic A1을 나무위키 페이지에 불러옴). 모양만 바뀐 거라 옛 북마크도 그대로 동작
  - 확인: 어두운 테마 데스크톱 화면 1회. 목표 글씨 대비 문제 발견 → 고침. 밝은 테마는 화면으로 확인 못 함 규칙상 페이지에서는 못 지우고, Firebase 콘솔 Firestore 데이터 탭에서 `rooms/claude-test`를 지우면 됨
